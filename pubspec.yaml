name: riverpod_mvvm_sample
description: A sample application which explain how to pass a value to the view model constructor.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^0.14.0+2
  freezed: ^0.14.1+3


dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed_annotation: ^0.14.1
  build_runner:
  pedantic_mono: ^1.11.0+2

flutter:
  uses-material-design: true
